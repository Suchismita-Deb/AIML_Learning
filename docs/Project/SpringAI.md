Spring project Maven - Dependency web, Open AI.

Default project run will give error as it need API key.

Spring AI and RAG.

The traditional LLM model works like - User asks the question to OpenAI/Grok/Gemini and it query the data in the pre trained data of LLM.
The problem with the approach - 

It rely on historical training data and not up-to-date data.

No knowledge about the internal document and knowledge base.


RAG is used to read external documents like the knowledge document, internal docs and use the LLM way to answer th question.
