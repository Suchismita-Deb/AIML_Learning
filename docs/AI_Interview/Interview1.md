### AI Engineer Interview.

What was your project?

Tell an Gen AI projet you have worked on - Document chatbot project using RAG, embedding and vectorDb and Langgraph.
When GPT is powerful then why to use RAG and embedding and vectorDb and Langgraph and not upload in gpt - context window, retrieval efficiency and token limit.

What was the challenges you faced when doing the poc and converting the poc to production project.

Why used the specific technology like langraph and why the unit++ model of deep learning and not anything else like unit model, mcp, yolo model?
When mentioned Langgraph then you should know what is nodes and edges.

Python Easy level dsa to see the language python, libraries, fastAPi, ML, AI, Agentic Ai, Gen AI and difference.

Basics and core concept of ML. What is backpropagation, forward propagation, gradient descent, loss function, activation function, optimizer, overfitting and underfitting, bias and variance, regularization, cross-validation, feature selection and engineering.

How model learns?
campusx ml and dl course.
Decision tree, random tree (random forest), and XGBoost and the difference.

Difference between MCP, Langraph and Langchain.
What is top k, sampling, temperature, and how it works in LLM.
what are metrics to see the accuracy of the model and the testing of the RAG.

What is orchestration of Agents.
what is similarity search, cosine similarity and how it works in LLM.
What is backtracking, can you explain it and explain the calculations behind it ?
What is the difference between decision trees , random forest and xg boosting ?
Why we use RAG if we can add a file and give it to llm to answer questions.
What is the difference between MCP and Langraph
How langraph and MCP can be used together
What metrics or score you will use to check the accuracy of RAG based answers.
What is Bleu and rogue score
What is Recall and Precision.
Why overlap?

Explain embedding without the use of the word vector?
Embedding convert the text into a mathematical representation to copare similar meaning efficiently.
### Scenario.

In which scenerio will you use recall ?
We already have Api then why was Mcp introduced what are the reasons.
You have deterministic documents of a company and employees uses enterprise level llm to answer their questions by uploading the documents. What are your views and what could be the ways so that employees get their answer from enterprise LLM without uploading the high secured documents.
Why was rag introduced We already have option to attach our doc to LLM
When the project is about the Rag and documentation project then what is the chunk size to use - It depends on the document type and in text 500-1000 word with overlapping of 100-200 words.

What will be the debugging approach when the chatbot starts giving wrong answers to the user.

Verify document chunking, retrieval quality, prompt structure and source relevance.

Can we remove halucination? - No. It can be reduced.

Explain everything that happens behind the scene of a RAG application?

User sends query and the query convert to the embeddings - similarity search runs and the relevant chunks retrieved, context added to the prompt and the LLM generates the answer and the response returns.

In the architecture think where do you think maximum latency happens?

What will happen when temp set to zero. 

The company data should not leave teh environment. What gen AI solution should be used?

Azure Ai, Open Ai, private deployment, access control, encryption and enterprise security.

Design a chatbot that answer 10k users and get the document from the internal knowledge base. How will you design the architecture? 



How do you scale the system when teh user is 10x?


What are the parameters of LLM and how they impact. 
What is an LLM?
 
Define the core principle and operational mechanism of LLM  
 
Parameter count - the significance of models with 300 billion plus parameters in achieving advanced capabilities.  
 
Tokenization and embeddings - how text is converted into numerical representations for model processing. 
 
Training phases - distinguish between pre training and fine tuning methodologies for LLM. 

### Transformer architecture - A deep dive into the transform architecture - the backbone of modern LLM.

Encoder versus decoder - understanding the role of each component in sequence to sequence tasks.  
 
Self attention - the mechanism that allows models to weigh the importance of different words in a sequence.  
 
Positional encoding - how Transformers incorporate sequence order without recurrent or convolutional layers.  
 
Why Transformers outperform - the advantages over traditional, RNN in parallelization and handling long range dependencies.

### Prompt Engineering.

Grafting effective prompts is a critical skills for maximizing LLM performance.  
 
Zero shot, one shot, few shot - leveraging different prompting strategies based on available examples  
Prompt templates - Designing reusable structures for consistent and effective interactions.

Controlling outputs - techniques for managing tone, format, mitigating hallucinations. 


Rag - Exploring rag's role in grounding LLM with external knowledge is for enhanced accuracy while rag is needed addressing limitations of standard LLM, rag VS fine tuning understand the trade-offs and optimal use cases, reducing hallucination, vector db.

### NLP concept for GenAI.

Call NLP techniques that underpin Gen. AI applications  
Stemming versus lematization  - Techniques for reducing water to their root forms.  
One hot encoding versus embedding - Representing categorical data and word meaning numerically.    
Cosine similarity   - Measuring semantic similarity for effective search and recommendation.

### GEN AI Evaluation.

Methods and metrics for assessing the quality and performance of genii models. 
BLEU and ROUGE scores - Automated metrics for evaluating text generated quality.  
Perplexity - A measure of how well a probability model predicts a sample.  
Human evaluation - incorporating subjective feedback loop for qualitative assessment.  
Safety and bias - crucial evaluation for ethical and responsible AI deployments 

### GENAI tools and Deployments.

Practical tools and platform essentials for building deploying and monitoring Gen. AI solutions. 
Land chain use cases - developing LLM powered applications with robust frameworks.  
Fast API for deployments- building high performance APIs for other integration.  
Model monitoring- tracking performance, drift and ensuring reliability in production.   
Cloud platform leveraging services like GCP Google clouds vertex AI for scalable solutions 

Share the real world experience on how you navigate the complex project scenarios.  

End to end genii or lack project detail your involvement and conceptions to deployment.   
Technology choices justify your selection of LLM, embeddings and evaluation metrics.   
Handling deadlines describe strategies for managing tight project schedule deliveries  
Rapid learning - illustrates your ability to quickly add up to and master new genii tools. 

What is the
https://www.youtube.com/watch?v=5v1_QyhiVXk
https://www.youtube.com/watch?v=Ykd5QkhqKbI

### Coding Example.

There are 3 list of numbers and get the union of the numbers.

Convert the list to set and get the union of the numbers.

You have 2 hash maps you need to merge only the distinct keys into one hash map.

With given a list you need flatten the list so that it is ml model consumable.

Leader in arrays leetcode question.

Palindrome string verify - algorithm design and efficiency considering for a string manipulation. 
 
Missing element - Identifying discrepancies between two lists using optimal approaches. 

List, string, dict and tuple.

Senior Software Engineer With Walmart living in Blr and total of 5 yoe and currently having knowledge in Linus, Devops.
